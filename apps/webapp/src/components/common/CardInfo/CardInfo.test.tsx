// Example Test
// TODO: Remove this file in the future once other tests have been written
describe("CardInfo", () => {
  it("should display text when clicked on ", () => {
    const one = 1;
    expect(one).toBe(1);
  });
});
